[package]
name = "validator"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
jwt-core = { path = "../../core/" }
risc0-zkvm = { version = "0.21.0", default-features = false, features = ["std"] }
